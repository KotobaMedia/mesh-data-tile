[package]
name = "mesh-data-tile-rs"
version = "0.1.0"
edition = "2021"
description = "Rust reference implementation for Mesh Tile Format v1 (MTI1)"
license = "MIT"
publish = false

[dependencies]
crc32fast = "1.4"
flate2 = { version = "1.0", default-features = false, features = ["rust_backend"] }
